# 成都理工大学校史馆｜极地科考滑轨屏联动播控系统开发文档 V1.2

## 0. 文档用途

本文档用于指导 Codex 开发一套部署在 **成都理工大学校史馆“极地科考”主题展陈区域** 的滑轨屏联动播控程序。

系统核心任务：

> 接收数字人或中控通过 Web API 发出的场景指令，控制滑轨屏移动到 4 个固定点位，并在到位后播放该点位对应的视频；同时支持 1 → 2 → 3 → 4 → 3 → 2 → 1 的往返自动轮播。

项目第一版重点：

- 功能简单、稳定；
- 前端视觉效果必须精美；
- UI 必须符合“极地科考 + 校史馆 + 成都理工大学”的展陈主题；
- 视频和装饰素材必须可外部替换；
- 用户后续会提供若干 Q 版恐龙/科考玩偶图片，作为页面边框、视频框、角标或状态装饰；
- 电机硬件协议目前可能尚未最终确定，因此第一版必须使用抽象接口并提供 Mock 模拟；
- 现有中控和数字人均可能使用 GET 或 POST，本项目控制接口必须同时兼容 GET + POST；
- 不开发复杂后台、数据库和内容管理系统。

Codex 必须严格按本文档 Phase 顺序开发。每个 Phase 完成后运行测试与构建并停止，等待人工评审。

---

# 1. 项目背景

## 1.1 展陈场景

项目部署位置：

```text
成都理工大学校史馆
└── 极地科考主题展区
```

现场有一块可沿滑轨移动的显示屏。

滑轨上设置 4 个固定展示点位：

```text
点位1
点位2
点位3
点位4
```

每个点位对应一段独立视频内容：

```text
点位1 → 视频1
点位2 → 视频2
点位3 → 视频3
点位4 → 视频4
```

屏幕移动到对应展项位置后，播放该点位视频。

---

# 2. 核心业务流程

## 2.1 数字人联动模式

数字人讲解过程中通过 Web API 指定场景。

例如数字人需要展示第 3 个极地科考展项：

```text
数字人
    ↓
GET /api/control/scene/3
    ↓
本程序收到指令
    ↓
停止或暂停当前视频
    ↓
控制滑轨移动到点位3
    ↓
等待电机到位
    ↓
播放视频3
```

数字人可以任意切换：

```text
1 → 4
4 → 2
2 → 3
```

不强制按照顺序。

---

## 2.2 中控联动模式

现有中控平台已经采用 GET 形式控制接口，例如：

```text
GET /api/control/play
GET /api/control/pause
GET /api/control/stop
GET /api/control/home
GET /api/control/video
GET /api/control/signature
GET /api/control/oath
```

本项目必须延续相同风格。

滑轨屏新增接口：

```text
GET /api/control/scene/1
GET /api/control/scene/2
GET /api/control/scene/3
GET /api/control/scene/4
```

数字人和中控都可以调用同一组接口。

---

## 2.3 自动往返轮播模式

系统支持自动巡展：

```text
1 → 2 → 3 → 4 → 3 → 2 → 1 → 2 → 3 → 4 ...
```

不是：

```text
1 → 2 → 3 → 4 → 1
```

即采用 Ping-Pong 往返模式。

每个点位流程：

```text
移动到目标点位
    ↓
等待到位
    ↓
播放当前视频
    ↓
视频播放结束
    ↓
计算下一个点位
    ↓
继续移动
```

方向逻辑：

```text
1 → 2 → 3 → 4
到4后方向改为 backward

4 → 3 → 2 → 1
到1后方向改为 forward
```

---

# 3. 第一版功能范围

## 3.1 必须完成

第一版必须完成：

- Windows 全屏运行；
- 单屏滑轨展示页面；
- 4 个固定场景；
- 每场景绑定一个视频；
- 数字人 GET API 控制；
- 中控 GET API 控制；
- 场景1～4切换；
- 上一个 / 下一个点位；
- 播放；
- 暂停；
- 停止；
- 回原点；
- 自动往返轮播；
- 停止自动轮播；
- 当前场景状态；
- 当前电机状态；
- 当前视频状态；
- Mock 电机模拟；
- 外部视频资源；
- 外部场景配置；
- Q版吉祥物装饰资源；
- 精美极地科考主题 UI；
- 视频播放异常处理；
- 电机异常降级；
- 日志；
- 开机启动；
- Kiosk / 全屏模式。

---

## 3.2 第一版暂时不做

第一版不开发：

- 数据库；
- 登录系统；
- 云端管理后台；
- 在线上传视频；
- 视频编辑；
- 多滑轨设备；
- 多屏同步；
- 数字人模型本身；
- 复杂拖拽页面编辑器；
- 动态创建无限场景；
- 用户权限系统；
- 历史统计报表。

---

# 4. Web API 设计

## 4.1 总体原则

本项目属于展厅局域网设备控制系统，调用方主要包括：

```text
数字人程序
中控平台
管理员测试工具
```

考虑到现有中控可能同时支持 GET 和 POST，本项目采用：

> **POST 作为推荐控制方式，GET 作为兼容方式。**

原因：

- POST 更符合“执行动作、改变设备状态”的 Web API 语义；
- GET 便于浏览器直接调试；
- 兼容已有中控项目的 GET 调用习惯；
- 数字人、中控以后可以逐步统一到 POST；
- 同一个路径同时支持 GET 和 POST，避免维护两套接口名称。

重要要求：

> GET 和 POST 只是不同入口，必须调用同一套业务 Service，绝不能各写一份场景控制逻辑。

例如：

```text
GET  /api/control/scene/3 ─┐
                           ├→ SceneService.activateScene(3)
POST /api/control/scene/3 ─┘
```

---

## 4.2 场景控制接口

每个场景同时支持：

```text
GET  /api/control/scene/1
POST /api/control/scene/1

GET  /api/control/scene/2
POST /api/control/scene/2

GET  /api/control/scene/3
POST /api/control/scene/3

GET  /api/control/scene/4
POST /api/control/scene/4
```

推荐数字人和新程序优先使用：

```text
POST /api/control/scene/3
```

中控或现场浏览器测试可以继续使用：

```text
GET /api/control/scene/3
```

成功返回示例：

```json
{
  "success": true,
  "accepted": true,
  "scene": 3,
  "message": "Scene 3 accepted"
}
```

如果已经位于场景3，或正在前往场景3：

```json
{
  "success": true,
  "accepted": false,
  "scene": 3,
  "message": "Scene 3 already active or pending"
}
```

这样可以避免重复驱动电机。

---

## 4.3 可选统一 POST 场景接口

为以后系统扩展，可额外保留：

```text
POST /api/control/scene
```

请求体：

```json
{
  "scene": 3
}
```

该接口不是第一版必须项。

如果实现，也必须继续调用：

```text
SceneService.activateScene(3)
```

不能形成第二套业务逻辑。

---

# 5. 播放控制接口

以下控制动作全部同时支持 GET 和 POST：

```text
GET  /api/control/play
POST /api/control/play

GET  /api/control/pause
POST /api/control/pause

GET  /api/control/stop
POST /api/control/stop

GET  /api/control/home
POST /api/control/home
```

说明：

### play

继续播放当前场景视频。

### pause

暂停当前视频。

### stop

停止当前视频。

### home

停止自动轮播和当前场景任务，并控制滑轨返回原点。

原点必须配置化，不得直接写死为固定物理位置。

---

# 6. 自动轮播接口

同时支持：

```text
GET  /api/control/carousel/start
POST /api/control/carousel/start

GET  /api/control/carousel/stop
POST /api/control/carousel/stop
```

启动后：

```text
1 → 2 → 3 → 4 → 3 → 2 → 1 → 2 ...
```

停止后：

- 停止继续自动切换；
- 保持当前物理位置；
- 默认保持当前视频状态。

如果之后数字人或中控发送某个场景指令：

```text
GET/POST /api/control/scene/4
```

则手动场景指令优先，自动轮播立即停止。

---

# 7. 查询类接口

查询状态只使用 GET：

```text
GET /api/status
```

因为该接口只是读取状态，不改变系统。

返回示例：

```json
{
  "currentScene": 2,
  "targetScene": 3,
  "motorState": "moving",
  "playbackState": "paused",
  "carouselMode": false,
  "carouselDirection": "forward",
  "videoId": "scene-2-video",
  "lastCommand": {
    "method": "POST",
    "path": "/api/control/scene/3"
  }
}
```

`motorState`：

```text
idle
moving
arrived
error
unknown
```

`playbackState`：

```text
idle
loading
playing
paused
stopped
error
```

---

## 7.1 API 方法使用约定

本项目统一约定：

```text
POST
→ 推荐用于控制动作

GET
→ 兼容中控、浏览器调试

GET
→ 唯一用于状态查询
```

因此：

```text
控制类接口：GET + POST 双支持
查询类接口：GET
```

第一版不使用 PUT、PATCH、DELETE 进行设备控制。

---

## 7.2 API 重复调用保护

GET 或 POST 都可能因为：

- 中控重复点击；
- 数字人重复发指令；
- 网络重试；
- 用户连续操作；

造成同一指令重复到达。

因此必须做幂等保护。

例如：

```text
当前 targetScene = 3
motorState = moving
```

再次收到：

```text
GET /api/control/scene/3
```

或：

```text
POST /api/control/scene/3
```

都不得再次调用真实电机移动命令。

同理：

```text
carousel 已经 running
```

再次调用：

```text
/api/control/carousel/start
```

直接返回当前状态，不重复启动任务。

---

# 8. 场景配置

场景和资源必须配置化。

推荐：

```text
config/scenes.json
```

示例：

```json
{
  "schemaVersion": "1.0",
  "homeScene": 1,
  "scenes": [
    {
      "id": 1,
      "name": "极地科考场景一",
      "subtitle": "科考启程",
      "videoPath": "content/videos/scene-1.mp4",
      "posterPath": "content/posters/scene-1.jpg",
      "motorPosition": "P1",
      "enabled": true
    },
    {
      "id": 2,
      "name": "极地科考场景二",
      "subtitle": "冰川探索",
      "videoPath": "content/videos/scene-2.mp4",
      "posterPath": "content/posters/scene-2.jpg",
      "motorPosition": "P2",
      "enabled": true
    },
    {
      "id": 3,
      "name": "极地科考场景三",
      "subtitle": "科学观测",
      "videoPath": "content/videos/scene-3.mp4",
      "posterPath": "content/posters/scene-3.jpg",
      "motorPosition": "P3",
      "enabled": true
    },
    {
      "id": 4,
      "name": "极地科考场景四",
      "subtitle": "科考精神",
      "videoPath": "content/videos/scene-4.mp4",
      "posterPath": "content/posters/scene-4.jpg",
      "motorPosition": "P4",
      "enabled": true
    }
  ]
}
```

正式场景名称以后可替换。

---

# 9. 外部资源设计

资源禁止全部写死进程序。

推荐部署目录：

```text
PolarRailDisplay/
├── PolarRailDisplay.exe
├── config/
│   ├── app.json
│   ├── scenes.json
│   └── machine.json
├── content/
│   ├── videos/
│   ├── posters/
│   ├── backgrounds/
│   ├── mascots/
│   ├── decorations/
│   └── logo/
└── logs/
```

说明：

```text
videos/
    四个场景的视频

posters/
    视频封面

backgrounds/
    极地科考背景

mascots/
    用户提供的Q版吉祥物PNG/WebP

decorations/
    雪花、冰晶、经纬线、科考元素等装饰

logo/
    成都理工大学或校史馆允许使用的视觉资源
```

所有路径必须使用相对路径。

---

# 10. Q版吉祥物资源设计

用户会单独提供若干 Q 版恐龙 / 科考玩偶图片。

程序必须允许这些素材直接替换。

推荐：

```text
content/mascots/
├── mascot-main.png
├── mascot-wave.png
├── mascot-book.png
├── mascot-tech.png
└── mascot-guide.png
```

前端使用场景：

- 左下角人物装饰；
- 右下角状态提示；
- 视频播放框边角；
- 首页边框；
- 自动轮播状态；
- 场景切换过渡；
- 无视频默认页；
- 设备错误友好提示。

注意：

- Q版玩偶只能作为装饰；
- 不允许遮挡视频主体；
- 不允许过量堆叠；
- 最多同时出现1～2个主要玩偶；
- 图片必须保持透明背景时优先使用 PNG / WebP；
- 不在代码中绑定固定图片名称，必须通过配置读取。

---

# 11. UI 视觉主题

## 11.1 核心视觉方向

本展项主题：

> 成都理工大学校史馆 × 极地科考

UI 不能做成普通播放器，也不能做成常见蓝紫色 AI 大屏。

视觉关键词：

```text
极地
冰川
雪原
科考
地质
测绘
探索
科研
校史
荣誉
使命
历史
```

整体气质：

```text
专业
高级
克制
沉浸
科技
冷峻
但不冰冷
```

---

# 12. 推荐配色

主色：

```text
深海蓝
冰川蓝
极地青
雪白
冷灰
```

强调色：

```text
低饱和橙红 / 科考服红
```

橙红可以与用户提供的 Q 版科考玩偶红色安全帽形成视觉呼应。

禁止：

- 大面积霓虹紫；
- 高饱和电竞蓝；
- 夸张赛博朋克；
- 大量廉价发光边框；
- 常见“AI科技蓝紫渐变”。

---

# 13. 页面视觉结构

推荐主页面：

```text
┌────────────────────────────────────────────────┐
│ 成都理工大学校史馆     极地科考                 │
│                                                │
│    ┌──────────────────────────────────────┐    │
│    │                                      │    │
│    │              主视频画面               │    │
│    │                                      │    │
│    │                                      │    │
│    └──────────────────────────────────────┘    │
│                                                │
│  Q版科考吉祥物          场景03 / 科学观测        │
│                        当前状态：播放中          │
│                                                │
│     ◀ 点位1   点位2   点位3   点位4 ▶          │
│                                                │
└────────────────────────────────────────────────┘
```

---

# 14. 视频框设计

视频区域是页面核心。

建议：

- 占屏幕约 70%～80%；
- 采用大圆角或微圆角；
- 视频外框使用冰川透明材质；
- 边框可使用细微冰晶纹理；
- 边角加入极轻量的经纬线 / 测绘坐标装饰；
- 不做厚重边框；
- 不影响视频可视面积。

视频边框可以使用：

```text
极地冰层
+
透明玻璃
+
细线测绘
+
科考UI
```

用户提供的 Q 版玩偶可以出现在视频框一个角落。

---

# 15. 背景设计

背景不要纯黑。

推荐：

- 深蓝极地渐变；
- 极淡冰川纹理；
- 极淡雪原层次；
- 经纬线；
- 地形等高线；
- 科考轨迹线；
- 低透明度雪粒。

这些元素必须很克制。

背景不能抢视频主体。

---

# 16. 顶部信息区

顶部显示：

```text
成都理工大学校史馆
极地科考
```

可选显示：

- 当前场景名称；
- 场景副标题；
- 当前点位。

不要做传统后台导航栏。

---

# 17. 场景点位指示

页面底部或侧边显示 4 个点位：

```text
01
02
03
04
```

当前点位突出显示。

建议视觉：

```text
○──●──○──○
1   2   3   4
```

滑轨移动时：

```text
当前位置 → 目标位置
```

可以使用轻微动态线条表示移动过程。

---

# 18. 状态显示

页面仅显示必要状态。

正常状态：

```text
正在前往点位 3
正在播放
自动巡展
```

不要显示：

```text
motorState=moving
HTTP 200
video index 2
```

技术状态只放管理员/调试页面。

---

# 19. 场景切换动画

切换时：

1. 当前视频淡出；
2. 页面显示“正在前往下一科考点位”；
3. Q版玩偶可做轻量提示装饰；
4. 等待电机；
5. 到位后新视频淡入。

允许：

- 淡入淡出；
- 轻微冰雾；
- 轻微横向滑动；
- 科考轨迹线移动。

禁止：

- 大面积粒子爆炸；
- 快速闪白；
- 复杂3D旋转；
- 影响现场性能的重动画。

---

# 20. 电机控制抽象

真实滑轨硬件协议可能尚未确定。

第一版必须定义：

```ts
interface MotorProvider {
  initialize(): Promise<void>;

  moveTo(position: string): Promise<void>;

  home(): Promise<void>;

  stop(): Promise<void>;

  getState(): Promise<MotorState>;

  waitUntilArrived(
    position: string,
    timeoutMs: number
  ): Promise<boolean>;

  dispose(): Promise<void>;
}
```

第一版实现：

```text
MockMotorProvider
```

后续可能增加：

```text
TcpMotorProvider
SerialMotorProvider
HttpMotorProvider
```

不得在硬件协议未知时编造真实控制命令。

---


# 20A. 真实硬件通信：当前仅预留占位

> **当前尚未拿到滑轨电机/控制器的正式通信协议，因此第一版不得假设、编造或写死任何真实硬件指令。**

现阶段开发目标是把“业务层”和“硬件通信层”彻底分开。

业务层只允许调用：

```ts
await motorProvider.moveTo("P1");
await motorProvider.moveTo("P2");
await motorProvider.moveTo("P3");
await motorProvider.moveTo("P4");
await motorProvider.home();
await motorProvider.stop();
```

当前开发阶段统一使用：

```text
MockMotorProvider
```

用于模拟：

```text
移动中
→ 延迟若干秒
→ 到位
→ 播放对应视频
```

后续拿到厂家协议后，只新增或替换具体硬件实现，例如：

```text
MotorProvider
├── MockMotorProvider        当前开发使用
├── TcpMotorProvider         预留
├── UdpMotorProvider         预留
├── SerialMotorProvider      预留
├── HttpMotorProvider        预留
└── PlcMotorProvider         预留
```

真实硬件接口可能包含：

- TCP；
- UDP；
- 串口 RS-232 / RS-485；
- HTTP；
- PLC；
- 厂商 SDK；
- 其他自定义协议。

在拿到正式协议前：

1. 不写真实报文；
2. 不写死 IP；
3. 不写死端口；
4. 不写死串口号；
5. 不假设是否存在“到位反馈”；
6. 不假设是否存在“当前位置查询”；
7. 不假设是否存在“急停”或“复位”命令；
8. 所有真实硬件参数均放在 `machine.json` 中预留配置；
9. 前端、Web API、SceneService、CarouselService 不得直接依赖任何具体硬件协议。

后续拿到协议时，只需要实现：

```text
具体 MotorProvider
+
machine.json 对应参数
```

不得重写：

- 页面 UI；
- Web API；
- 场景逻辑；
- 视频播放逻辑；
- 自动轮播逻辑。



# 20B. API 与硬件通信层必须解耦

Web API 的请求方式与未来电机厂家协议没有直接关系。

例如：

```text
POST /api/control/scene/3
```

只是外部系统调用本程序的方式。

程序内部仍然是：

```text
API
↓
SceneService
↓
MotorProvider.moveTo("P3")
↓
未来真实硬件协议
```

因此未来滑轨厂家即使提供：

```text
TCP
UDP
RS-485
PLC
HTTP
SDK
```

也只修改 `MotorProvider` 实现。

不得因为真实电机协议变化而修改：

- 数字人 Web API；
- 中控 Web API；
- 前端 UI；
- SceneService；
- MediaService；
- CarouselService。


# 21. Mock 电机行为

为了在硬件未到时完成开发：

例如：

```text
P1 → P2：2秒
P2 → P3：2秒
P3 → P4：2秒
```

开发模式调用：

```text
GET /api/control/scene/3
```

页面：

```text
正在移动到点位3
```

Mock 延迟结束：

```text
motorState = arrived
```

然后自动播放视频3。

等待时间必须配置化。

---

# 22. 到位后再播放

重要原则：

> 不建议收到场景 API 后立即开始播放视频。

正确流程：

```text
收到scene 3
    ↓
停止当前视频
    ↓
MotorProvider.moveTo(P3)
    ↓
等待到位
    ↓
到位成功
    ↓
MediaService.play(scene3)
```

如果后续电机提供真实到位反馈，则使用真实反馈。

如果没有反馈，可使用可配置等待时间。

---

# 23. MediaService

所有视频操作统一通过：

```ts
interface MediaService {
  load(sceneId: number): Promise<void>;
  play(): Promise<void>;
  pause(): Promise<void>;
  stop(): Promise<void>;
  getState(): PlaybackState;
}
```

禁止：

- API路由直接操作 video DOM；
- 场景按钮自己维护播放状态；
- 自动轮播和数字人控制各写一套播放器代码。

---

# 24. SceneService

所有场景切换统一经过：

```ts
interface SceneService {
  activateScene(sceneId: number): Promise<void>;
  goHome(): Promise<void>;
  getCurrentScene(): number | null;
  getTargetScene(): number | null;
}
```

核心流程：

```text
API
   ↓
SceneService
   ├── MotorProvider
   └── MediaService
```

---

# 25. CarouselService

自动巡展逻辑单独管理：

```ts
interface CarouselService {
  start(): Promise<void>;
  stop(): Promise<void>;
  isRunning(): boolean;
}
```

状态：

```text
currentScene
direction
running
```

算法：

```text
if direction == forward:
    1 → 2 → 3 → 4
    到4后 direction = backward

if direction == backward:
    4 → 3 → 2 → 1
    到1后 direction = forward
```

---

# 26. 指令冲突处理

数字人可能在自动轮播过程中发出场景指令。

优先级：

```text
数字人/中控手动场景指令
>
自动轮播
```

例如：

```text
自动轮播正在 2 → 3

数字人调用：
GET /api/control/scene/4
```

程序：

1. 停止自动轮播；
2. 取消当前未完成场景任务；
3. 移动到4；
4. 播放视频4。

---

# 27. 重复命令处理

例如连续调用：

```text
GET /api/control/scene/3
GET /api/control/scene/3
GET /api/control/scene/3
```

如果已经：

```text
currentScene = 3
```

则不重复驱动电机。

如果已经：

```text
targetScene = 3
motorState = moving
```

则忽略重复命令。

避免电机被重复触发。

---

# 28. 错误处理

## 28.1 视频缺失

- 显示场景海报；
- 显示“当前内容暂不可用”；
- 不影响电机位置；
- 记录日志。

## 28.2 视频播放失败

- 重试一次；
- 仍失败则显示封面；
- 记录日志。

## 28.3 电机无法到位

- 不播放正式场景视频；
- 显示“设备正在调整，请稍候”；
- 记录错误；
- 保留人工重试入口。

## 28.4 电机状态未知

- 不默认认为已经到位；
- 页面保持等待状态。

## 28.5 API收到非法场景

例如：

```text
GET /api/control/scene/9
```

返回：

```json
{
  "success": false,
  "error": "INVALID_SCENE"
}
```

---

# 29. 技术方案

推荐：

```text
React
+
TypeScript
+
FastAPI
+
PyInstaller
```

整体：

```text
React 前端
      ↓
FastAPI 本地服务
      ↓
SceneService
├── Media控制状态
├── CarouselService
└── MotorProvider
```

Windows EXE 启动后：

```text
启动 FastAPI
    ↓
监听局域网端口
    ↓
打开全屏播放器
```

如果现有系列项目已经采用 WebView2 / Electron 壳层，也可复用，但第一版不要为了外观引入过度复杂架构。

核心要求是：

- Web API 稳定；
- 浏览器/全屏壳均可展示；
- 电机层可替换。

---

# 30. API 服务监听

建议：

```text
0.0.0.0:8000
```

允许：

- 本机调用；
- 数字人主机调用；
- 中控平板调用。

正式部署可在配置中修改：

```json
{
  "apiHost": "0.0.0.0",
  "apiPort": 8000
}
```

---

# 31. 管理/调试模式

正式界面不显示复杂管理控件。

管理员快捷键：

```text
Ctrl + Shift + Alt + M
```

管理页提供：

- 场景1～4测试；
- 电机模拟；
- 原点；
- 播放；
- 暂停；
- 停止；
- 自动轮播启动；
- 自动轮播停止；
- 查看 API 地址；
- 查看当前状态；
- 视频文件检查；
- 内容重新加载；
- 查看日志。

---

# 32. 响应式要求

优先适配：

```text
1920 × 1080
```

同时适配常见横屏比例。

要求：

- 主视频不变形；
- Q版素材不被裁掉；
- 控制区不会越界；
- 状态文字不会遮挡视频人物；
- 低分辨率下仍保持完整。

---

# 33. 开发阶段

## Phase 1：前端视觉 Demo

第一阶段重点只做视觉和基础播放器壳。

完成：

- 极地科考主题主页；
- 成都理工大学校史馆视觉定位；
- 视频大框；
- 极地背景；
- 场景点位1～4；
- 当前场景信息；
- 状态显示；
- Q版吉祥物占位素材接口；
- 示例视频；
- 淡入淡出切换；
- 全屏适配。

Codex 必须优先打磨视觉。

不要先做硬件。

完成后提供页面截图或可运行预览。

停止等待评审。

---

## Phase 2：视频和场景逻辑

完成：

- scenes.json；
- 外部视频；
- 场景1～4；
- 播放；
- 暂停；
- 停止；
- SceneService；
- MediaService；
- 内容重新加载；
- 视频异常处理。

停止等待评审。

---

## Phase 3：Web API

完成以下接口的 GET + POST 双兼容：

```text
GET/POST /api/control/scene/1
GET/POST /api/control/scene/2
GET/POST /api/control/scene/3
GET/POST /api/control/scene/4

GET/POST /api/control/play
GET/POST /api/control/pause
GET/POST /api/control/stop
GET/POST /api/control/home

GET/POST /api/control/carousel/start
GET/POST /api/control/carousel/stop

GET /api/status
```

要求：

- POST 作为推荐控制方式；
- GET 保留兼容和浏览器调试；
- GET 与 POST 必须调用同一个 Service；
- 不允许复制两套业务逻辑；
- 数字人可测试；
- 中控可测试；
- 浏览器 GET 可直接测试；
- POST 可通过 curl / Postman / 数字人程序测试；
- 同场景重复请求必须幂等保护；
- API 不直接操作播放器或电机；
- 统一调用 SceneService / MediaService / CarouselService。

停止等待评审。

---

## Phase 4：Mock滑轨和自动轮播

完成：

- MotorProvider；
- MockMotorProvider；
- 假移动时间；
- 到位后播放；
- 往返轮播；
- 1→2→3→4→3→2→1；
- 数字人指令打断自动轮播；
- 重复命令保护。

停止等待评审。

---

## Phase 5：打包和现场准备

完成：

- Windows EXE；
- 外部 config；
- 外部 content；
- 日志；
- 开机启动；
- 全屏；
- Kiosk；
- API局域网访问；
- 连续运行测试；
- 视频替换测试；
- Q版素材替换测试；
- API调用测试。

---

## Phase 6：真实电机接入

> 本 Phase 只有在拿到滑轨厂家正式通信协议后才执行。当前开发阶段只保留接口占位，不实现真实通信。


等待滑轨厂商协议。

确认硬件属于：

```text
TCP
UDP
串口
HTTP
PLC
其他
```

再实现对应：

```text
MotorProvider
```

这一阶段不得提前编造协议。

---

# 34. Codex UI 设计要求

Phase 1 开始前，Codex 必须先做视觉分析。

要求：

> 可自行搜索和参考高质量极地科考、博物馆、科学探索、地质科研类网站或数字展陈项目的视觉设计，但只能借鉴视觉语言，不得照搬品牌、图片、Logo或代码。

重点参考：

- 极地科研视觉；
- 科考队装备；
- 冰川；
- 科学测绘；
- 地质调查；
- 博物馆数字展陈；
- 高质量纪录片标题系统。

前端不得有：

- Bootstrap后台味；
- 普通视频播放器味；
- 管理系统味；
- 电商卡片味；
- 常见AI蓝紫渐变。

---

# 35. Q版玩偶融入规则

用户后续会将真实 Q 版玩偶图片放入：

```text
content/mascots/
```

Codex 第一版使用占位素材。

正式替换时无需修改布局代码。

推荐配置：

```json
{
  "mascots": {
    "main": "content/mascots/mascot-main.png",
    "moving": "content/mascots/mascot-moving.png",
    "playing": "content/mascots/mascot-playing.png",
    "error": "content/mascots/mascot-error.png"
  }
}
```

应用方式：

```text
待机 → 主吉祥物
移动 → 科考/挥手形象
播放 → 安静陪伴形象
异常 → 提示形象
```

---

# 36. Codex 开发约束

Codex 必须遵守：

1. 每次只完成一个 Phase；
2. 每阶段完成后停止等待评审；
3. 前端视觉质量优先；
4. 极地科考主题必须明确；
5. 必须体现成都理工大学校史馆展陈属性；
6. 视频、图片、Q版玩偶不得写死；
7. 正式资源必须外置；
8. 第一版不使用数据库；
9. 控制类 API 同时支持 GET + POST，POST 为推荐方式，GET 保持兼容；
10. 场景切换统一经过 SceneService；
11. 电机统一通过 MotorProvider；
12. 播放统一通过 MediaService；
13. 自动轮播统一通过 CarouselService；
14. 未知硬件协议不得编造；
15. 第一版必须有 MockMotorProvider；
16. 必须到位后再播放视频；
17. 手动API指令优先于自动轮播；
18. 重复点位请求必须防重复执行；
19. 不增加无关复杂功能；
20. 每阶段执行类型检查、测试、构建。

---

# 37. 第一版验收标准

必须满足：

- 程序可全屏运行；
- UI 符合极地科考主题；
- UI 视觉精美；
- 有成都理工大学校史馆展陈感；
- Q版玩偶可以作为外部素材替换；
- 4个场景可独立配置；
- 4个视频可独立替换；
- scene/1～4 的 GET 与 POST 均正常；
- play / pause / stop 的 GET 与 POST 均正常；
- home 正常；
- 自动轮播正常；
- 顺序严格为 1→2→3→4→3→2→1；
- 数字人指令可以中断自动轮播；
- Mock电机正常；
- 当前未拿到硬件协议时，程序可完全使用 MockMotorProvider 开发和演示；
- 到位后播放正常；
- 重复命令不会重复驱动；
- 视频缺失不会白屏；
- API 状态查询正常；
- 局域网可以访问 API；
- 可生成 Windows EXE。

---

# 38. 给 Codex 的首次提示词

```text
请完整阅读 docs/成都理工大学校史馆极地科考滑轨屏联动播控系统开发文档V1.0.md。

这是成都理工大学校史馆“极地科考”主题展项的软件。

当前先不要做真实滑轨硬件协议，硬件尚未最终确认。使用 MockMotorProvider 占位。

前端视觉效果是第一优先级：
- 必须具有极地科考、冰川、科学探索、地质科研、校史馆数字展陈气质；
- 不要做成普通播放器；
- 不要做成后台管理页面；
- 不要采用廉价蓝紫AI大屏风格；
- 视频是页面主体；
- 后续我会提供多张Q版科考恐龙/玩偶PNG，请预留资源位，用于视频边框、页面边角和状态装饰；
- 所有正式视频和图片必须放在外部content目录并可替换。
- 控制类 Web API 必须同时支持 GET + POST；POST 作为推荐方式，GET 用于兼容现有中控和浏览器调试；
- GET 与 POST 只能作为不同入口，必须调用同一套业务 Service，严禁写两套控制逻辑；

可以自行搜索和参考高质量极地科考、科学博物馆、数字展陈类网站的UI设计，只借鉴视觉语言，不复制素材和品牌。

严格按照Phase顺序开发，每次只完成一个Phase。

现在只执行 Phase 1：
先完成UI视觉方案分析，再直接实现高质量前端Demo。
完成后运行类型检查和构建，提供运行方式、主要修改文件、视觉设计说明和截图/预览，然后停止等待我评审。
```

---

# 39. 后续提示词

Phase 1 评审通过后：

```text
Phase 1评审通过。

保持当前已经确认的极地科考视觉风格，不要重新设计UI。

继续执行Phase 2，只实现视频和场景逻辑。
完成测试和构建后停止等待评审。
```

后续以同样方式逐阶段执行。
